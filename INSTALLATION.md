# 🛠️ Инструкция по установке MovesLive

## Предварительные требования

Перед началом работы убедитесь, что у вас установлено:

- **Visual Studio 2019** или **Visual Studio 2022**
- **.NET Framework 4.7.2** или выше
- **Windows 7/8/10/11**
- **Подключение к интернету** (для загрузки NuGet пакетов)

---

## Шаг 1: Клонирование репозитория

### С помощью Git

```bash
git clone https://github.com/MaminHaker228/MovesLive.git
cd MovesLive
```

### Или скачайте ZIP-архив

1. Перейдите на [https://github.com/MaminHaker228/MovesLive](https://github.com/MaminHaker228/MovesLive)
2. Нажмите на зеленую кнопку `Code`
3. Выберите `Download ZIP`
4. Распакуйте архив в нужное место

---

## Шаг 2: Открытие проекта в Visual Studio

1. Откройте **Visual Studio**
2. Выберите `File` → `Open` → `Project/Solution`
3. Перейдите в папку, куда вы клонировали/распаковали проект
4. Откройте файл `MovesLive.sln`

---

## Шаг 3: Восстановление NuGet пакетов

### Автоматический способ (рекомендуется)

Visual Studio автоматически восстановит пакеты при первой сборке проекта.

### Ручной способ

Если автоматическое восстановление не сработало:

1. Нажмите правой кнопкой мыши на `Solution` в **Solution Explorer**
2. Выберите `Restore NuGet Packages`

Или:

1. Перейдите в `Tools` → `NuGet Package Manager` → `Package Manager Console`
2. Выполните команду:
   ```powershell
   Update-Package -reinstall
   ```

### Необходимые пакеты

Проект использует следующие NuGet пакеты:

- **System.Data.SQLite.Core** (v1.0.118.0) - База данных SQLite
- **Newtonsoft.Json** (v13.0.3) - Работа с JSON

---

## Шаг 4: Сборка проекта

### Через меню

1. В верхнем меню выберите `Build` → `Build Solution`
2. Или нажмите `Ctrl + Shift + B`

### Проверка успешной сборки

В нижней части Visual Studio в окне **Output** должно появиться:

```
Build succeeded.
0 Error(s)
0 Warning(s)
```

---

## Шаг 5: Запуск приложения

### Запуск в режиме отладки

1. Нажмите `F5` или кнопку `Start` (зеленый треугольник)

### Запуск без отладки

1. Нажмите `Ctrl + F5`

---

## Шаг 6: Первый вход в систему

При первом запуске приложения:

1. Автоматически создастся база данных `Data/moveslive.db`
2. Импортируются тестовые фильмы
3. Создается тестовый пользователь

### Тестовые данные для входа

- **Логин:** `admin`
- **Пароль:** `123456`

---

## Возможные проблемы и решения

### Проблема: Не найдены NuGet пакеты

**Решение:**

1. Проверьте подключение к интернету
2. В **Solution Explorer** нажмите правой кнопкой на `Solution`
3. Выберите `Restore NuGet Packages`
4. Подождите завершения загрузки

### Проблема: Ошибки компиляции

**Решение:**

1. Убедитесь, что .NET Framework 4.7.2 установлен
2. Очистите и пересоберите проект:
   - `Build` → `Clean Solution`
   - `Build` → `Rebuild Solution`

### Проблема: База данных не создается

**Решение:**

1. Убедитесь, что у приложения есть права на запись в папку
2. Проверьте, что папка `Data/` существует рядом с .exe файлом
3. Проверьте логи ошибок в консоли

### Проблема: Видео не воспроизводится

**Решение:**

1. Проверьте подключение к интернету (фильмы стримятся онлайн)
2. Убедитесь, что в системе есть необходимые кодеки

---

## Сборка Release версии

Для создания оптимизированной версии:

1. В верхней панели Visual Studio измените `Debug` на `Release`
2. Нажмите `Build` → `Build Solution`
3. Скомпилированное приложение будет в папке `bin/Release/`

---

## Создание инсталлятора (опционально)

Для распространения приложения можно создать инсталлятор с помощью:

- **Inno Setup** (https://jrsoftware.org/isinfo.php)
- **WiX Toolset** (https://wixtoolset.org/)
- **ClickOnce** (встроенный в Visual Studio)

---

## Дополнительные ресурсы

- [README.md](README.md) - Общая информация о проекте
- [LICENSE](LICENSE) - Лицензия MIT
- [GitHub Issues](https://github.com/MaminHaker228/MovesLive/issues) - Сообщение об ошибках

---

## Полезные команды Visual Studio

| Комбинация | Действие |
|------------|----------|
| `Ctrl + Shift + B` | Собрать проект |
| `F5` | Запустить с отладкой |
| `Ctrl + F5` | Запустить без отладки |
| `Shift + F5` | Остановить отладку |
| `F9` | Поставить точку останова |
| `F10` | Шаг через (перешагнуть) |
| `F11` | Шаг в (войти в метод) |

---

Успешной работы с MovesLive! 🎬✨