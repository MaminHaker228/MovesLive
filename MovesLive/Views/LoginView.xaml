<UserControl x:Class="MovesLive.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая часть с логотипом -->
        <StackPanel Grid.Column="0" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Margin="50">
            <TextBlock Text="MovesLive" 
                       Style="{StaticResource HeaderText}"
                       FontSize="48"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Ваш личный онлайн кинотеатр" 
                       Style="{StaticResource SecondaryText}"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Центральная часть с формой входа -->
        <Border Grid.Column="1" 
                Background="{StaticResource SecondaryBackgroundBrush}"
                CornerRadius="20"
                Padding="40"
                VerticalAlignment="Center"
                Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                BlurRadius="30" 
                                ShadowDepth="10" 
                                Opacity="0.4"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Вход в систему" 
                           Style="{StaticResource HeaderText}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Логин -->
                <TextBlock Text="Логин" 
                           Style="{StaticResource BodyText}"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="UsernameTextBox"
                         Style="{StaticResource ModernTextBox}"
                         Margin="0,0,0,20"/>

                <!-- Пароль -->
                <TextBlock Text="Пароль" 
                           Style="{StaticResource BodyText}"
                           Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordBox"
                             Style="{StaticResource ModernPasswordBox}"
                             Margin="0,0,0,10"/>

                <!-- Сообщение об ошибке -->
                <TextBlock x:Name="ErrorTextBlock"
                           Foreground="{StaticResource ErrorBrush}"
                           FontSize="12"
                           Margin="0,0,0,20"
                           TextWrapping="Wrap"
                           Visibility="Collapsed"/>

                <!-- Кнопка входа -->
                <Button x:Name="LoginButton"
                        Content="Войти"
                        Style="{StaticResource ModernButton}"
                        Margin="0,10,0,0"
                        Padding="0,15"
                        Click="LoginButton_Click"/>

                <!-- Информация для теста -->
                <Border Background="{StaticResource CardBrush}"
                        CornerRadius="10"
                        Padding="15"
                        Margin="0,30,0,0">
                    <StackPanel>
                        <TextBlock Text="Тестовый аккаунт:" 
                                   Style="{StaticResource SecondaryText}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,5"/>
                        <TextBlock Style="{StaticResource SecondaryText}">
                            <Run Text="Логин:"/>
                            <Run Text="admin" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource SecondaryText}">
                            <Run Text="Пароль:"/>
                            <Run Text="123456" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Правая часть (декоративная) -->
        <StackPanel Grid.Column="2" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Margin="50">
            <TextBlock Text="🎬" 
                       FontSize="80"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            <TextBlock Text="Более 10 фильмов" 
                       Style="{StaticResource SecondaryText}"
                       FontSize="14"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="доступно сразу после входа" 
                       Style="{StaticResource SecondaryText}"
                       FontSize="14"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>